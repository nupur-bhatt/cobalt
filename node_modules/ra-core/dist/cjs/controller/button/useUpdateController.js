"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useUpdateController = void 0;
var react_1 = require("react");
var useUpdate_1 = require("../../dataProvider/useUpdate");
var useRedirect_1 = require("../../routing/useRedirect");
var useNotify_1 = require("../../notification/useNotify");
var useRecordContext_1 = require("../record/useRecordContext");
var useResourceContext_1 = require("../../core/useResourceContext");
var useTranslate_1 = require("../../i18n/useTranslate");
/**
 * Prepare a set of callbacks for an update button
 *
 * @example
 * const UpdateButton = ({
 *     resource,
 *     record,
 *     redirect,
 *     ...rest
 * }) => {
 *     const {
 *         isPending,
 *         handleUpdate,
 *     } = useUpdateController({
 *         mutationMode: 'pessimistic',
 *         resource,
 *         record,
 *         redirect,
 *     });
 *
 *     const [open, setOpen] = useState(false);
 *
 *     return (
 *         <Fragment>
 *             <Button
 *                 onClick={() => setOpen(true)}
 *                 label="ra.action.update"
 *                 {...rest}
 *             >
 *                 {icon}
 *             </Button>
 *             <Confirm
 *                 isOpen={open}
 *                 loading={isPending}
 *                 title="ra.message.update_title"
 *                 content="ra.message.update_content"
 *                 titleTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 contentTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 onConfirm={() => handleUpdate()}
 *                 onClose={() => setOpen(false)}
 *             />
 *         </Fragment>
 *     );
 * };
 */
var useUpdateController = function (props) {
    var _a = props.redirect, redirectTo = _a === void 0 ? false : _a, _b = props.mutationMode, mutationMode = _b === void 0 ? 'undoable' : _b, _c = props.mutationOptions, mutationOptions = _c === void 0 ? {} : _c, successMessage = props.successMessage;
    var mutationMeta = mutationOptions.meta, otherMutationOptions = __rest(mutationOptions, ["meta"]);
    var record = (0, useRecordContext_1.useRecordContext)(props);
    var resource = (0, useResourceContext_1.useResourceContext)(props);
    var notify = (0, useNotify_1.useNotify)();
    var redirect = (0, useRedirect_1.useRedirect)();
    var translate = (0, useTranslate_1.useTranslate)();
    var _d = (0, useUpdate_1.useUpdate)(resource, undefined, __assign({ onSuccess: function () {
            notify(successMessage !== null && successMessage !== void 0 ? successMessage : "resources.".concat(resource, ".notifications.updated"), {
                type: 'info',
                messageArgs: {
                    smart_count: 1,
                    _: translate('ra.notification.updated', {
                        smart_count: 1,
                    }),
                },
                undoable: mutationMode === 'undoable',
            });
            redirect(redirectTo, resource);
        }, onError: function (error) {
            notify(typeof error === 'string'
                ? error
                : (error === null || error === void 0 ? void 0 : error.message) || 'ra.notification.http_error', {
                type: 'error',
                messageArgs: {
                    _: typeof error === 'string'
                        ? error
                        : error === null || error === void 0 ? void 0 : error.message,
                },
            });
        }, mutationMode: mutationMode }, otherMutationOptions)), updateOne = _d[0], isPending = _d[1].isPending;
    var handleUpdate = (0, react_1.useCallback)(function (data) {
        if (!record) {
            throw new Error('The record cannot be updated because no record has been passed');
        }
        updateOne(resource, {
            id: record.id,
            data: data,
            previousData: record,
            meta: mutationMeta,
        });
    }, [updateOne, mutationMeta, record, resource]);
    return (0, react_1.useMemo)(function () { return ({
        isPending: isPending,
        isLoading: isPending,
        handleUpdate: handleUpdate,
    }); }, [isPending, handleUpdate]);
};
exports.useUpdateController = useUpdateController;
//# sourceMappingURL=useUpdateController.js.map