"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var __1 = require("../");
var routing_1 = require("../../routing");
var notification_1 = require("../../notification");
var core_1 = require("../../core");
var i18n_1 = require("../../i18n");
var util_1 = require("../../util");
/**
 * Prepare a set of callbacks for a delete button guarded by confirmation dialog
 * @deprecated prefer the useDeleteController hook instead
 *
 * @example
 *
 * const DeleteButton = ({
 *     resource,
 *     record,
 *     redirect,
 *     onClick,
 *     ...rest
 * }) => {
 *     const {
 *         open,
 *         isPending,
 *         handleDialogOpen,
 *         handleDialogClose,
 *         handleDelete,
 *     } = useDeleteWithConfirmController({
 *         resource,
 *         record,
 *         redirect,
 *         onClick,
 *     });
 *
 *     return (
 *         <Fragment>
 *             <Button
 *                 onClick={handleDialogOpen}
 *                 label="ra.action.delete"
 *                 {...rest}
 *             >
 *                 {icon}
 *             </Button>
 *             <Confirm
 *                 isOpen={open}
 *                 loading={isPending}
 *                 title="ra.message.delete_title"
 *                 content="ra.message.delete_content"
 *                 titleTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 contentTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 onConfirm={handleDelete}
 *                 onClose={handleDialogClose}
 *             />
 *         </Fragment>
 *     );
 * };
 */
var useDeleteWithConfirmController = function (props) {
    var mutationMode = props.mutationMode, onClick = props.onClick, record = props.record, _a = props.redirect, redirectTo = _a === void 0 ? 'list' : _a, successMessage = props.successMessage, _b = props.mutationOptions, mutationOptions = _b === void 0 ? {} : _b, rest = __rest(props, ["mutationMode", "onClick", "record", "redirect", "successMessage", "mutationOptions"]);
    var _c = (0, react_1.useState)(false), open = _c[0], setOpen = _c[1];
    var resource = (0, core_1.useResourceContext)(props);
    var notify = (0, notification_1.useNotify)();
    var unselect = (0, __1.useUnselect)(resource);
    var redirect = (0, routing_1.useRedirect)();
    var translate = (0, i18n_1.useTranslate)();
    var _d = (0, __1.useDeleteController)(__assign({ mutationMode: mutationMode, mutationOptions: __assign({ onSuccess: function () {
                setOpen(false);
                notify(successMessage !== null && successMessage !== void 0 ? successMessage : "resources.".concat(resource, ".notifications.deleted"), {
                    type: 'info',
                    messageArgs: {
                        smart_count: 1,
                        _: translate('ra.notification.deleted', {
                            smart_count: 1,
                        }),
                    },
                    undoable: mutationMode === 'undoable',
                });
                record && unselect([record.id]);
                redirect(redirectTo, resource);
            }, onError: function (error) {
                setOpen(false);
                notify(typeof error === 'string'
                    ? error
                    : (error === null || error === void 0 ? void 0 : error.message) ||
                        'ra.notification.http_error', {
                    type: 'error',
                    messageArgs: {
                        _: typeof error === 'string'
                            ? error
                            : (error === null || error === void 0 ? void 0 : error.message)
                                ? error.message
                                : undefined,
                    },
                });
            } }, mutationOptions), record: record, redirect: redirectTo, successMessage: successMessage }, rest)), isPending = _d.isPending, controllerHandleDelete = _d.handleDelete;
    var handleDialogOpen = (0, util_1.useEvent)(function (e) {
        e.stopPropagation();
        setOpen(true);
    });
    var handleDialogClose = (0, util_1.useEvent)(function (e) {
        e.stopPropagation();
        setOpen(false);
    });
    var handleDelete = (0, util_1.useEvent)(function (event) {
        if (event && event.stopPropagation) {
            event.stopPropagation();
        }
        controllerHandleDelete();
        if (typeof onClick === 'function') {
            onClick(event);
        }
    });
    return {
        open: open,
        isPending: isPending,
        isLoading: isPending,
        handleDialogOpen: handleDialogOpen,
        handleDialogClose: handleDialogClose,
        handleDelete: handleDelete,
    };
};
exports.default = useDeleteWithConfirmController;
//# sourceMappingURL=useDeleteWithConfirmController.js.map